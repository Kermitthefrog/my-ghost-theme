{{!< default}}

<main class="content" role="main">
   <center><h1 class="post-title">#{{tag.name}}</h1></center>

  {{#foreach posts}}
    <article class="{{post_class}}">
      <header class="post-header">
        <h2 class="post-title"><a href="{{url}}">{{{title}}}</a></h2>
      </header>

      <section class="tag-post-excerpt">
        <p>{{excerpt words="40"}}</p>
        <a class="read-more" href="{{url}}">Läs mer&raquo;</a>
      </section>
    </article>
  {{/foreach}}

  <section class="share">
    <div class="left">
      <a href="/#blog"><i class="fa fa-angle-left "></i>Tillbaka</a>
    </div>
  </section>

  <center>
    {{pagination}}
  </center>
</main>
{{> footer}}